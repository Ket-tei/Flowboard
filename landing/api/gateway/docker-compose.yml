services:
  gateway:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./instances:/etc/nginx/instances:ro
      - ./logs:/var/log/flowboard
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - flowboard_gateway

networks:
  flowboard_gateway:
    external: true
    name: flowboard_gateway
